<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background shape -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/calc_card_white" />
            <stroke android:width="1dp" android:color="@color/calc_border_gray" />
            <corners android:radius="4dp" />
        </shape>
    </item>

    <!-- Vertical separator line -->
    <item
        android:left="0dp"
        android:right="48dp">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
        </shape>
    </item>

    <item
        android:width="1dp"
        android:gravity="end">
        <shape android:shape="rectangle">
            <solid android:color="@color/calc_border_gray" />
        </shape>
    </item>

    <!-- Dropdown arrow -->
    <item
        android:gravity="end|center_vertical"
        android:right="12dp">
        <bitmap
            android:src="@android:drawable/arrow_down_float"
            android:tileMode="disabled" />
    </item>

</layer-list>
